import { ActivitySortPipe } from './activity-sort.pipe';

describe('ActivitySortPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivitySortPipe();
    expect(pipe).toBeTruthy();
  });
});
